const FREE_DB_BYTES=500*1024*1024;
const fmt=n=>new Intl.NumberFormat('pt-BR').format(n||0);
const size=bytes=>bytes<1024*1024?`${(bytes/1024).toFixed(1)} KB`:`${(bytes/1024/1024).toFixed(1)} MB`;
async function loadHealth(){const user=await window.authReady;if(!user)return;if(user.profile.role!=='admin'){location.replace('vendas.html');return;}const{data,error}=await window.supabaseClient.rpc('get_system_health');if(error){document.querySelector('#healthState').textContent='Não disponível';document.querySelector('#healthMessage').textContent='Não foi possível consultar os indicadores agora.';return;}const pct=Math.max(0,(Number(data.database_bytes||0)/FREE_DB_BYTES)*100);document.querySelector('#dbSize').textContent=size(data.database_bytes);document.querySelector('#dbPercent').textContent=`${pct.toFixed(1).replace('.',',')}% do limite Free`;document.querySelector('#salesCount').textContent=fmt(data.sales_count);document.querySelector('#profilesCount').textContent=fmt(data.profiles_count);document.querySelector('#notificationsCount').textContent=fmt(data.notifications_count);document.querySelector('#dbUsageLabel').textContent=`${size(data.database_bytes)} / 500 MB`;document.querySelector('#dbUsageBar').style.width=`${Math.min(100,pct)}%`;const state=document.querySelector('#healthState'),message=document.querySelector('#healthMessage');if(pct<70){state.innerHTML='<i></i> Normal';state.className='health-state normal';message.textContent='Uso confortável. Nenhuma ação necessária no momento.';}else if(pct<90){state.innerHTML='<i></i> Atenção';state.className='health-state warning';message.textContent='O banco está se aproximando do limite do plano Free. Vale acompanhar o crescimento.';}else{state.innerHTML='<i></i> Crítico';state.className='health-state critical';message.textContent='O banco está próximo do limite do plano Free. Considere limpeza ou mudança de plano.';}document.querySelector('#healthUpdated').textContent=`Atualizado em ${new Date(data.measured_at).toLocaleString('pt-BR')}`;}
document.querySelector('#menuButton').addEventListener('click',()=>document.querySelector('#sidebar').classList.toggle('open'));loadHealth();