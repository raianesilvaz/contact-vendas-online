window.authReady?.then(user=>{if(!user)return;const role=user.profile?.role;if(!['vendedora','admin'].includes(role)){location.replace('painel.html');return}if(role==='admin'){document.querySelectorAll('[data-admin-only]').forEach(el=>el.hidden=false)}document.querySelectorAll('.conversation-tabs button').forEach(button=>button.addEventListener('click',()=>{document.querySelectorAll('.conversation-tabs button').forEach(b=>b.classList.remove('active'));button.classList.add('active')}))});